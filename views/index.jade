extends layout

block content
  p
    form(action="login" method="post")
      div 
        input(required type='text', name='username', placeholder='用户名')
      div
        input(required type='password', name='password', placeholder='密码')
      div
        button.sign(type='submit') 登录
  p  #{info}
extends layout
block append scripts
    //script.
        $(document).ready(function(){
            for(var i=2;i<9;i++){
                $.get("/mazeInfo/"+i,function(data,status){
                    alert(i);
                    $('#tdStatus'+ i).text(data);
                });
            }
        });
    script.
        $(document).ready(function(){
            for(var i=2;i<9;i++){
               (function(mapStageId){
                   $.get("/mazeInfo/"+ mapStageId,function(data,status){
                    $('#lblMazeInfo'+ mapStageId).text(data);
                });
               })(i);
            }
        });
block content
    h1= "魔卡幻想一键"
    div
        p
            table
                tr
                    th 迷宫
                    th 状态
                    th 操作
                - for(var i=2;i<9;i++)
                    tr
                        td #{i}塔
                        td
                            label( id = "lblMazeInfo#{ i }")
                        td
                            button 一键
                            button 重置

    div
        p #{info}
